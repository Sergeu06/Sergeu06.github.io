[build]
  publish = "public" # Папка с вашими статическими файлами (фронтенд)
  functions = "netlify/functions" # Папка с функциями

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
